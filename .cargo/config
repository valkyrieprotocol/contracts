[alias]
wasm = "build --workspace --release --target wasm32-unknown-unknown"
wasm-debug = "build --workspace --target wasm32-unknown-unknown"
unit-test = "test --workspace --lib --features backtraces"
integration-test = "test --workspace --test integration"
